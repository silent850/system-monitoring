[phases.setup]
nixPkgs = ['chromium', 'nss', 'freetype', 'harfbuzz', 'ca-certificates', 'wget']

[phases.install]
cmds = ['npm install --production']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'node dist/server.cjs'
